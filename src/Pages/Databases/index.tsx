import { Button } from 'antd';
import * as React from 'react';

class Databases extends React.PureComponent {
  public render() {
    return (
      <div>
        <Button type={'primary'}>Databases</Button>
      </div>
    );
  }
}

export default Databases;
